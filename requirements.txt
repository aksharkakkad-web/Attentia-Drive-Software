opencv-python>=4.8.0
numpy>=1.24.0
PyYAML>=6.0
mediapipe>=0.10.9
# tflite-runtime not available for Python 3.10 on macOS — mediapipe bundles TFLite internally
# tflite-runtime>=2.14.0; platform_system != "Windows"
tensorflow>=2.14.0; platform_system == "Windows"
pytest>=7.0.0
